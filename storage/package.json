{
  "main": "./dist/node.js",
  "browser": "./dist/browser.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf ./dist",
    "compile": "tsc -p .",
    "build": "npm run clean && npm run compile"
  }
}
